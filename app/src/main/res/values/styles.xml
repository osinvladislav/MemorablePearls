<resources>

    <!-- Base application theme. -->
    <style name="MainTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="error" parent="@style/TextAppearance.Design.HelperText">
        <item name="android:textColor">@color/color_error</item>
    </style>

    <style name="ok" parent="@style/TextAppearance.Design.HelperText">
        <item name="android:textColor">@color/color_ok</item>
    </style>

    <style name="standart" parent="@style/TextAppearance.Design.HelperText"></style>

</resources>
